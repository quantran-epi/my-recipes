---
phase: 01-measurement-and-performance-harness
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/e2e/fixtures/performanceSeed.ts
  - tests/e2e/fixtures/performanceNetwork.ts
  - tests/e2e/fixtures/seedApp.ts
  - tests/e2e/fixtures/appTest.ts
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Maintainer can seed daily and stress datasets that include ingredients, dishes, shopping lists, inventory batches, scheduled meals, dish ingredients, included dishes, and mixed image states."
    - "Existing e2e tests keep their current default regression seed when no performance dataset option is supplied."
    - "Performance tests can select online normal, browser offline, and mocked slow/blocked GitHub plus image modes per D-10, D-11, D-12, and D-13."
  artifacts:
    - path: "tests/e2e/fixtures/performanceSeed.ts"
      provides: "Daily and stress dataset generator"
      exports:
        - "PERFORMANCE_DATASET_SIZES"
        - "createPerformanceSeed"
    - path: "tests/e2e/fixtures/performanceNetwork.ts"
      provides: "Controlled GitHub/image/offline network modes"
      exports:
        - "applyPerformanceNetworkMode"
    - path: "tests/e2e/fixtures/seedApp.ts"
      provides: "Parameterized app seeding"
      exports:
        - "seedApp"
  key_links:
    - from: "tests/e2e/fixtures/seedApp.ts"
      to: "tests/e2e/fixtures/performanceSeed.ts"
      via: "dataset option selects createRegressionSeed or createPerformanceSeed"
      pattern: "createPerformanceSeed"
    - from: "tests/e2e/fixtures/seedApp.ts"
      to: "tests/e2e/fixtures/performanceNetwork.ts"
      via: "network options install Playwright routes before app bootstrap"
      pattern: "applyPerformanceNetworkMode"
---

<objective>
Add deterministic large-list seed coverage and controlled network/image modes.

Purpose: PERF-01 needs daily and stress data that reproduces large-list behavior without using real user data. The user's offline-speed concern also needs selectable online/offline/network modes before timing specs can compare them.
Output: Performance dataset and network fixtures consumed by Phase 1 measurement specs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-and-performance-harness/01-CONTEXT.md
@.planning/phases/01-measurement-and-performance-harness/01-PATTERNS.md
@tests/e2e/fixtures/testData.ts
@tests/e2e/fixtures/seedApp.ts
@tests/e2e/fixtures/appTest.ts
@src/Store/Store.ts
@src/Store/Models/Ingredient.ts
@src/Store/Models/Dishes.ts
@src/Store/Models/ShoppingList.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create daily and stress performance datasets</name>
  <files>tests/e2e/fixtures/performanceSeed.ts</files>
  <read_first>tests/e2e/fixtures/testData.ts, src/Store/Store.ts, src/Store/Models/Ingredient.ts, src/Store/Models/Dishes.ts, src/Store/Models/ShoppingList.ts, .planning/phases/01-measurement-and-performance-harness/01-CONTEXT.md</read_first>
  <action>Follow the deterministic ID, helper, and Redux Persist shape patterns from `tests/e2e/fixtures/testData.ts` identified in `01-PATTERNS.md`. Create `tests/e2e/fixtures/performanceSeed.ts` exporting `PerformanceDatasetName`, `PERFORMANCE_DATASET_SIZES`, and `createPerformanceSeed(datasetName: PerformanceDatasetName)`. Implement D-01, D-02, and D-03 exactly: `daily` creates about 200 ingredients, 150 dishes, and 100 shopping lists; `stress` creates about 1000 ingredients, 750 dishes, and 500 shopping lists. Implement D-04 by generating realistic relationships: inventory batches for a meaningful subset of ingredients, shopping-list ingredient groups, scheduled meals, dish ingredients, and some dishes with `includeDishes`. Implement D-05 by assigning a mixed dish image set: a small number of data/local-like images, a small number of remote URL images, and most dishes with no image/fallback state. Keep IDs deterministic with stable prefixes such as `perf-daily-ing-0001`, `perf-stress-dish-0001`, and `perf-daily-sl-0001`.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures/performanceSeed.ts` exports `PERFORMANCE_DATASET_SIZES` with `daily.ingredients` equal to `200`, `daily.dishes` equal to `150`, `daily.shoppingLists` equal to `100`, `stress.ingredients` equal to `1000`, `stress.dishes` equal to `750`, and `stress.shoppingLists` equal to `500`.
    - `tests/e2e/fixtures/performanceSeed.ts` exports `createPerformanceSeed`.
    - Generated seed shape contains `shared.ingredient.ingredients`, `shared.dishes.dishes`, `personal.inventory.items`, `personal.shoppingList.shoppingLists`, `personal.scheduledMeal.scheduledMeals`, and `personal.cookingSession.sessions`.
    - Generated dishes include at least one non-empty `includeDishes` array.
    - Generated shopping lists include non-empty `ingredients`, `dishes`, and `scheduledMeals` arrays.
    - Generated dish images include remote `https://` strings, local/data-like strings, and empty/undefined image states.
  </acceptance_criteria>
  <done>Daily and stress performance seeds are deterministic and represent the large-list relationship graph.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize app seeding and network modes</name>
  <files>tests/e2e/fixtures/performanceNetwork.ts, tests/e2e/fixtures/seedApp.ts, tests/e2e/fixtures/appTest.ts</files>
  <read_first>tests/e2e/fixtures/seedApp.ts, tests/e2e/fixtures/appTest.ts, tests/e2e/fixtures/performanceSeed.ts, src/Hooks/useSharedDataSync.ts, src/Components/AppInitializer/SharedSyncModal.tsx, src/serviceWorkerRegistration.ts</read_first>
  <action>Follow the `page.route`, `page.addInitScript`, localStorage envelope, and service-worker cleanup patterns from `tests/e2e/fixtures/seedApp.ts` identified in `01-PATTERNS.md`. Create `tests/e2e/fixtures/performanceNetwork.ts` exporting `PerformanceNetworkMode`, `PerformanceImageMode`, and `applyPerformanceNetworkMode(page, options)`. Support D-10 modes `online-normal`, `browser-offline`, and `mocked-slow-network`. Preserve D-11 by stubbing GitHub Raw by default unless `PERF_REAL_NETWORK=1` or an explicit `realNetwork` option is set. Implement D-12 image modes `fast`, `slow`, and `blocked` by routing remote image URLs with configurable delay. Preserve D-13 by keeping service-worker unregister/cache clearing as the normal default. Update `seedApp(page, options?)` so existing tests still call `seedApp(page)` and get `createRegressionSeed()`, while performance specs can request `dataset: 'daily' | 'stress'`, `networkMode`, and `imageMode`. Update `appTest.ts` only if needed to keep the default fixture unchanged.</action>
  <verify>npm run test:e2e -- --list && npm run test:e2e:performance -- --list</verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures/performanceNetwork.ts` exports `applyPerformanceNetworkMode`.
    - `tests/e2e/fixtures/performanceNetwork.ts` contains the mode strings `online-normal`, `browser-offline`, and `mocked-slow-network`.
    - `tests/e2e/fixtures/performanceNetwork.ts` contains the image mode strings `fast`, `slow`, and `blocked`.
    - `tests/e2e/fixtures/seedApp.ts` keeps `seedApp(page)` valid for existing tests.
    - `tests/e2e/fixtures/seedApp.ts` supports a dataset option that calls `createPerformanceSeed` for `daily` and `stress`.
    - `tests/e2e/fixtures/seedApp.ts` still clears service worker registrations and cache storage by default.
    - `npm run test:e2e -- --list` exits 0.
  </acceptance_criteria>
  <done>Performance specs can select dataset and network modes while current e2e tests remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture generator to browser localStorage | Generated persisted state is trusted by app reducers/selectors. |
| Playwright route interception to app network calls | Tests simulate GitHub/image/offline behavior that could mask real-network issues if mislabeled. |
| Service-worker cleanup to browser cache | Tests clear browser-controlled storage before app startup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-T | Tampering | `performanceSeed.ts` persisted Redux payloads | mitigate | Match `src/Store/Store.ts` persisted slice keys and keep deterministic IDs so invalid shape failures surface in Playwright startup. |
| T-01-02-R | Repudiation | network mode labels | mitigate | Store selected `dataset`, `networkMode`, and `imageMode` in performance evidence generated by later specs. |
| T-01-02-I | Information Disclosure | fixture setup | mitigate | Do not read real `.env` or user localStorage; `page.addInitScript` must clear browser storage before seeding. |
| T-01-02-D | Denial of Service | stress dataset generation | mitigate | Generate data synchronously once per test setup and keep image-heavy rows limited per D-05. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build` exits 0 or existing unrelated warnings are documented.
- [ ] `npm run test:e2e -- --list` exits 0.
- [ ] Default e2e fixture behavior remains unchanged for existing specs.
</verification>

<success_criteria>
- Daily and stress seeds exist and match D-01 through D-05.
- Online/offline/mock network and image modes exist and match D-10 through D-13.
- Existing regression seed behavior is preserved.
</success_criteria>

## Artifacts this phase produces

- `tests/e2e/fixtures/performanceSeed.ts`
- `PerformanceDatasetName`
- `PERFORMANCE_DATASET_SIZES`
- `createPerformanceSeed`
- `tests/e2e/fixtures/performanceNetwork.ts`
- `PerformanceNetworkMode`
- `PerformanceImageMode`
- `applyPerformanceNetworkMode`
- `SeedAppOptions`

<output>
After completion, create `.planning/phases/01-measurement-and-performance-harness/01-02-SUMMARY.md`.
</output>
