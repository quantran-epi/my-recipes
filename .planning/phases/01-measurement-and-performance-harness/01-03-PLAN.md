---
phase: 01-measurement-and-performance-harness
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/e2e/fixtures/performanceReport.ts
  - tests/e2e/performance-baseline.spec.ts
  - tests/e2e/performance-regression.spec.ts
  - src/Routing/MasterPage.tsx
  - src/Modules/Ingredient/Screens/IngredientList.screen.tsx
  - src/Modules/Dishes/Screens/DishesList.screen.tsx
  - src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx
  - docs/performance-audit-plan.md
  - docs/automated-regression-test-plan.md
autonomous: true
requirements:
  - PERF-02
  - TEST-01
  - UX-01
must_haves:
  truths:
    - "Maintainer can capture shell-visible and content-ready timing for modal shell, drawer/sidebar, row menu, detail route navigation, and search/filter reset interactions."
    - "Performance evidence writes JSON and markdown summaries under `test-results/performance/` and records strict UX target misses as warnings during Phase 1."
    - "Baseline documentation names the proposed smoke budgets that later phases must preserve or improve."
  artifacts:
    - path: "tests/e2e/fixtures/performanceReport.ts"
      provides: "Shared timing and evidence writer helpers"
      exports:
        - "measureInteraction"
        - "writePerformanceEvidence"
    - path: "tests/e2e/performance-baseline.spec.ts"
      provides: "Opt-in baseline timing/resource comparison"
      contains: "PERF_BASELINE"
    - path: "tests/e2e/performance-regression.spec.ts"
      provides: "Generous smoke regression timing checks"
      contains: "measureInteraction"
    - path: "docs/performance-audit-plan.md"
      provides: "Stable Phase 1 command and budget summary"
  key_links:
    - from: "tests/e2e/performance-baseline.spec.ts"
      to: "tests/e2e/fixtures/performanceSeed.ts"
      via: "baseline runs select daily/stress datasets"
      pattern: "createPerformanceSeed|PERF_DATASET"
    - from: "tests/e2e/performance-baseline.spec.ts"
      to: "tests/e2e/fixtures/performanceNetwork.ts"
      via: "baseline runs compare online/offline/mocked network modes"
      pattern: "PERF_NETWORK_MODE|applyPerformanceNetworkMode"
    - from: "tests/e2e/performance-regression.spec.ts"
      to: "tests/e2e/fixtures/performanceReport.ts"
      via: "regression spec writes JSON evidence and warnings"
      pattern: "writePerformanceEvidence"
---

<objective>
Capture baseline timing/resource evidence and document Phase 1 smoke budgets.

Purpose: PERF-02 and UX-01 require actionable evidence for the reported large-list interaction delays and online/offline difference. Phase 1 should measure and warn, not fix or strictly gate the final UX target yet.
Output: Shared performance measurement helpers, updated performance specs, optional diagnostic profiling, stable docs, and proposed smoke budgets.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-and-performance-harness/01-CONTEXT.md
@.planning/phases/01-measurement-and-performance-harness/01-PATTERNS.md
@tests/e2e/performance-baseline.spec.ts
@tests/e2e/performance-regression.spec.ts
@tests/e2e/fixtures/performanceSeed.ts
@tests/e2e/fixtures/performanceNetwork.ts
@src/Components/Modal/Modal.tsx
@src/Routing/MasterPage.tsx
@src/Modules/Ingredient/Screens/IngredientList.screen.tsx
@src/Modules/Dishes/Screens/DishesList.screen.tsx
@src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx
@docs/performance-audit-plan.md
@docs/automated-regression-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared timing and evidence helpers</name>
  <files>tests/e2e/fixtures/performanceReport.ts, tests/e2e/performance-baseline.spec.ts, tests/e2e/performance-regression.spec.ts</files>
  <read_first>tests/e2e/performance-baseline.spec.ts, tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceSeed.ts, tests/e2e/fixtures/performanceNetwork.ts, .planning/phases/01-measurement-and-performance-harness/01-CONTEXT.md</read_first>
  <action>Follow the Node `fs`/`path`, Playwright `testInfo.attach`, `PerformanceResourceTiming`, and click-to-visible timing patterns from `tests/e2e/performance-baseline.spec.ts` and `tests/e2e/performance-regression.spec.ts` identified in `01-PATTERNS.md`. Create `tests/e2e/fixtures/performanceReport.ts` exporting `measureInteraction`, `summarizeResources`, `writePerformanceEvidence`, and `writePerformanceSummaryMarkdown`. Implement D-06 by keeping Phase 1 baseline-first: strict 100 ms shell targets are recorded as warnings, not failing assertions. Implement D-07 and D-09 by storing both `shellVisibleMs` and `contentReadyMs` when content-ready target locators exist. Implement D-14 and D-15 by writing JSON plus a short markdown summary under `test-results/performance/`. Implement D-17 by keeping CPU profile and trace hooks disabled unless `PERF_DIAGNOSTIC=1`.</action>
  <verify>npm run test:e2e:performance -- --list</verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures/performanceReport.ts` exports `measureInteraction`.
    - `tests/e2e/fixtures/performanceReport.ts` exports `writePerformanceEvidence`.
    - Evidence JSON includes fields `dataset`, `networkMode`, `imageMode`, `interactions`, `warnings`, `budgets`, and `capturedAt`.
    - Interaction records include `shellVisibleMs` and `contentReadyMs` fields.
    - Markdown summaries are written under `test-results/performance/`.
    - CPU profile or trace capture is gated by `PERF_DIAGNOSTIC`.
  </acceptance_criteria>
  <done>Measurement helpers can record shell/content timing and warning-based evidence without strict UX failures.</done>
</task>

<task type="auto">
  <name>Task 2: Measure required large-list interactions and comparison modes</name>
  <files>tests/e2e/performance-baseline.spec.ts, tests/e2e/performance-regression.spec.ts, src/Routing/MasterPage.tsx, src/Modules/Ingredient/Screens/IngredientList.screen.tsx, src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx</files>
  <read_first>tests/e2e/performance-baseline.spec.ts, tests/e2e/performance-regression.spec.ts, src/Routing/MasterPage.tsx, src/Modules/Ingredient/Screens/IngredientList.screen.tsx, src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx, src/Components/Modal/Modal.tsx</read_first>
  <action>Follow the route-case array, locator, resource summary, and evidence-writing patterns from the current `tests/e2e/performance-baseline.spec.ts` and `tests/e2e/performance-regression.spec.ts` identified in `01-PATTERNS.md`. Update the baseline and regression specs to measure every D-08 interaction class: modal shell, drawer/sidebar, row menu, detail route navigation, and search/filter reset. Use daily/stress datasets from Plan 02 and comparison modes from D-10, D-11, D-12, and D-13: online normal, browser offline, and mocked slow/blocked GitHub plus image network. Keep normal regression assertions generous per D-18, failing setup/runtime errors and broad smoke thresholds only. If selectors are unreliable, add nonvisual hooks only: `data-testid="sidebar-drawer-button"`, `data-testid="dish-row-menu-{id}"`, `data-testid="shopping-list-row-menu-{id}"`, and similar stable hooks for search reset/filter reset. Do not change visual layout, row heights, modal behavior, sync behavior, image loading behavior, or service-worker behavior in this plan.</action>
  <verify>npm run test:e2e:performance && npm run test:e2e:performance:baseline</verify>
  <acceptance_criteria>
    - `tests/e2e/performance-baseline.spec.ts` records at least one interaction with id containing `modal`.
    - `tests/e2e/performance-baseline.spec.ts` records at least one interaction with id containing `drawer` or `sidebar`.
    - `tests/e2e/performance-baseline.spec.ts` records at least one interaction with id containing `row-menu`.
    - `tests/e2e/performance-baseline.spec.ts` records at least one interaction with id containing `detail-route`.
    - `tests/e2e/performance-baseline.spec.ts` records at least one interaction with id containing `search-reset` or `filter-reset`.
    - `tests/e2e/performance-baseline.spec.ts` references both `daily` and `stress` datasets or accepts `PERF_DATASET` with documented defaults for both.
    - `tests/e2e/performance-baseline.spec.ts` references `online-normal`, `browser-offline`, and `mocked-slow-network`.
    - Source UI changes, if any, are limited to nonvisual `data-testid` or `aria-label` hooks for measurement selectors.
  </acceptance_criteria>
  <done>Phase 1 specs measure the requested interaction classes across deterministic dataset and network modes.</done>
</task>

<task type="auto">
  <name>Task 3: Document evidence, commands, and proposed smoke budgets</name>
  <files>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md</files>
  <read_first>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md, .planning/phases/01-measurement-and-performance-harness/01-CONTEXT.md, tests/e2e/fixtures/performanceReport.ts</read_first>
  <action>Follow the existing work-item, test-matrix, and reporting-summary documentation patterns from `docs/performance-audit-plan.md` and `docs/automated-regression-test-plan.md` identified in `01-PATTERNS.md`. Update docs with stable Phase 1 commands and budget policy. Implement D-16 by documenting commands, output paths, JSON/markdown summary schema, and proposed smoke budgets, not every raw run. Implement D-18 by stating that strict UX target misses warn in Phase 1 evidence and later phases may turn them into failing gates. Include the target from D-07: shell visible under 100 ms as the desired UX goal. Include current command names from Plan 01: `npm run test:e2e:performance`, `npm run test:e2e:performance:baseline`, and `npm run test:e2e:performance:diagnostic`.</action>
  <verify>rg -n "test:e2e:performance|test-results/performance|shell visible|100 ms|warnings" docs/performance-audit-plan.md docs/automated-regression-test-plan.md</verify>
  <acceptance_criteria>
    - `docs/performance-audit-plan.md` contains `npm run test:e2e:performance`.
    - `docs/performance-audit-plan.md` contains `test-results/performance/`.
    - `docs/performance-audit-plan.md` states that Phase 1 strict UX target misses are warnings, not failures.
    - `docs/automated-regression-test-plan.md` contains `npm run test:e2e:performance:baseline`.
    - `docs/automated-regression-test-plan.md` contains `npm run test:e2e:performance:diagnostic`.
    - At least one doc names the desired shell-visible target as `100 ms` or `under 100ms`.
  </acceptance_criteria>
  <done>Maintainers know how to run the harness, find evidence, and interpret warning versus failure budgets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser timing API to evidence files | Performance entries and measured durations are trusted for baseline reporting. |
| Test selectors to source UI | Nonvisual hooks may be added to production source for measurement stability. |
| Real network opt-in to GitHub/image hosts | Optional real-network baseline can contact external services. |
| Evidence docs to future phase gates | Proposed budgets can become enforceable later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-T | Tampering | `performanceReport.ts` evidence files | mitigate | Include captured mode metadata, command, budgets, and warnings in every JSON output. |
| T-01-03-R | Repudiation | timing baseline docs | mitigate | Write machine-readable JSON plus markdown summaries with `capturedAt` and command fields. |
| T-01-03-I | Information Disclosure | baseline evidence and docs | mitigate | Do not include `.env` values, GitHub tokens, or user real localStorage data in evidence. |
| T-01-03-D | Denial of Service | stress baseline | mitigate | Keep normal regression to generous smoke checks and reserve stress/diagnostic runs for opt-in commands. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e:performance` exits 0 or exact runtime blocker is documented.
- [ ] `npm run test:e2e:performance:baseline` writes JSON and markdown evidence under `test-results/performance/`.
- [ ] `npm run test:e2e:performance:diagnostic` enables diagnostic artifacts without changing normal run behavior.
- [ ] `npm run build` exits 0 or existing unrelated warnings are documented.
</verification>

<success_criteria>
- Modal, drawer/sidebar, row menu, detail navigation, and search/filter reset timings are captured.
- Shell-visible and content-ready timings are separate in evidence.
- Online/offline/network comparison modes are represented in evidence.
- Docs name stable commands, output paths, warning policy, and smoke budgets.
</success_criteria>

## Artifacts this phase produces

- `tests/e2e/fixtures/performanceReport.ts`
- `measureInteraction`
- `summarizeResources`
- `writePerformanceEvidence`
- `writePerformanceSummaryMarkdown`
- evidence field `shellVisibleMs`
- evidence field `contentReadyMs`
- evidence field `warnings`
- `data-testid="sidebar-drawer-button"`
- `data-testid="dish-row-menu-{id}"`
- `data-testid="shopping-list-row-menu-{id}"`

<output>
After completion, create `.planning/phases/01-measurement-and-performance-harness/01-03-SUMMARY.md`.
</output>
