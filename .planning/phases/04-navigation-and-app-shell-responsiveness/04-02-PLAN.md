---
phase: 04-navigation-and-app-shell-responsiveness
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - tests/e2e/performance-regression.spec.ts
  - tests/e2e/app-shell-navigation.spec.ts
  - docs/performance-audit-plan.md
  - docs/automated-regression-test-plan.md
  - docs/large-list-modal-sidebar-performance-note.md
autonomous: true
requirements:
  - LIST-04
  - UX-02
must_haves:
  truths:
    - "D-13: drawer open/close, drawer navigation, bottom-tab navigation, global-search navigation, large-list detail navigation, and key modal behavior are verified after Phase 4."
    - "D-14: Phase 4 uses both automated timing checks and manual eye checks with simple user-facing evidence."
    - "D-15 and UX-02: tests/docs prove drawer tools and daily cooking/shopping/search workflows are preserved, not simplified."
    - "D-16 and LIST-04: the visible success signal is immediate drawer shell and route feedback from large-list screens."
    - "D-17: the final evidence docs emphasize a short visual checklist and timing summary instead of a technical-only report."
  artifacts:
    - path: "tests/e2e/performance-regression.spec.ts"
      provides: "Phase 4 app-shell navigation timing gate"
      contains: "perf-10-phase4-app-shell-navigation"
    - path: "tests/e2e/app-shell-navigation.spec.ts"
      provides: "UX preservation checks for deferred drawer tools and route feedback cleanup"
      contains: "sidebar-drawer-tools"
    - path: "docs/performance-audit-plan.md"
      provides: "Phase 4 budgets, commands, evidence, and visual checks"
      contains: "Phase 4"
    - path: "docs/automated-regression-test-plan.md"
      provides: "Phase 4 regression matrix entry and command guidance"
      contains: "PERF-SHELL"
    - path: "docs/large-list-modal-sidebar-performance-note.md"
      provides: "Updated non-technical explanation of remaining visible checks after Phase 4"
      contains: "What You Can Check By Eye"
  key_links:
    - from: "tests/e2e/performance-regression.spec.ts"
      to: "tests/e2e/fixtures/performanceReport.ts"
      via: "measureInteraction writes shell/content timing evidence for Phase 4 interactions"
      pattern: "writePerformanceEvidence"
    - from: "tests/e2e/app-shell-navigation.spec.ts"
      to: "src/Routing/MasterPage.tsx"
      via: "test ids verify drawer shell, deferred tools, and route feedback cleanup"
      pattern: "sidebar-drawer-tools"
    - from: "docs/performance-audit-plan.md"
      to: "test-results/performance/perf-10-phase4-app-shell-navigation.md"
      via: "docs point maintainers/users to the generated Phase 4 evidence"
      pattern: "perf-10-phase4-app-shell-navigation"
---

<objective>
As a My Recipes maintainer and user, I want Phase 4 app-shell responsiveness to be measurable and easy to check by eye so that future rich feature, UI, UX, and performance work does not regress drawer/navigation responsiveness or remove daily workflows.

Purpose: Plan 04-01 changes the app-shell behavior. This plan proves the behavior with timing evidence, regression coverage, and documentation that the user can inspect in simple language.
Output: Phase 4 performance evidence, focused app-shell regression coverage, and updated documentation with visual checks and practical budgets.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-CONTEXT.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-RESEARCH.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-VALIDATION.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-UI-SPEC.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-PATTERNS.md
@.planning/phases/04-navigation-and-app-shell-responsiveness/04-01-PLAN.md
@tests/e2e/performance-regression.spec.ts
@tests/e2e/fixtures/performanceReport.ts
@tests/e2e/fixtures/seedApp.ts
@tests/e2e/global-search.spec.ts
@tests/e2e/dish-serving-and-modal.spec.ts
@docs/performance-audit-plan.md
@docs/automated-regression-test-plan.md
@docs/large-list-modal-sidebar-performance-note.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 app-shell timing evidence</name>
  <files>tests/e2e/performance-regression.spec.ts</files>
  <read_first>tests/e2e/performance-regression.spec.ts, tests/e2e/performance-baseline.spec.ts, tests/e2e/fixtures/performanceReport.ts, tests/e2e/fixtures/seedApp.ts, .planning/phases/04-navigation-and-app-shell-responsiveness/04-VALIDATION.md, .planning/phases/04-navigation-and-app-shell-responsiveness/04-PATTERNS.md, .planning/phases/04-navigation-and-app-shell-responsiveness/04-UI-SPEC.md</read_first>
  <action>Implement D-13, D-14, and D-16 by extending `tests/e2e/performance-regression.spec.ts` with a Phase 4 daily app-shell navigation test that runs when the normal performance command runs and is skipped only in Phase 3 comparison mode. Add a `phase4Budgets` object with `shellTargetMs: 100`, `drawerShellMs: 1000`, `routeFeedbackMs: 1000`, and `routeContentMs: 5000`. Measure at least these interactions with `measureInteraction`: `phase4-drawer-shell-open` from `dishes/list` using `sidebar-drawer-button`, shell locator `sidebar-drawer-primary-nav`, and content locator `sidebar-drawer-tools`; `phase4-drawer-route-navigation` by clicking a drawer route link and using `app-route-feedback` as shell and a destination list test id as content; `phase4-bottom-tab-navigation` using the bottom tab route path; `phase4-global-search-navigation` by clicking an existing global search result; and `phase4-dish-detail-route-navigation` or `phase4-shopping-list-detail-route-navigation` from a large-list detail modal. Write evidence with `writePerformanceEvidence` to `perf-10-phase4-app-shell-navigation` and include notes that 100 ms remains ideal warning evidence while practical shell/content budgets are enforced. Keep all existing Phase 2 and Phase 3 tests and budgets active.</action>
  <verify><automated>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance</automated></verify>
  <acceptance_criteria>
    - `performance-regression.spec.ts` contains `phase4Budgets` with `drawerShellMs: 1000`, `routeFeedbackMs: 1000`, and `routeContentMs: 5000`.
    - `performance-regression.spec.ts` contains interaction ids `phase4-drawer-shell-open`, `phase4-drawer-route-navigation`, `phase4-bottom-tab-navigation`, `phase4-global-search-navigation`, and at least one `phase4-*-detail-route-navigation` id.
    - The Phase 4 test uses `data-testid="app-route-feedback"` as the shell locator for route-feedback interactions.
    - The Phase 4 evidence file base name is `perf-10-phase4-app-shell-navigation`.
    - The strict daily performance command exits 0 or records exact failing interaction ids and timings for follow-up in the summary.
  </acceptance_criteria>
  <done>Phase 4 has repeatable shell-visible/content-ready evidence for drawer and navigation paths from large-list screens.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused UX preservation and stale-overlay regression checks</name>
  <files>tests/e2e/app-shell-navigation.spec.ts</files>
  <read_first>tests/e2e/global-search.spec.ts, tests/e2e/dish-serving-and-modal.spec.ts, tests/e2e/fixtures/appTest.ts, tests/e2e/fixtures/testData.ts, src/Routing/MasterPage.tsx, src/Modules/Home/Screens/GlobalSearch.screen.tsx, .planning/phases/04-navigation-and-app-shell-responsiveness/04-UI-SPEC.md</read_first>
  <action>Create `tests/e2e/app-shell-navigation.spec.ts` using the existing `appTest` fixture. Implement D-13 and D-15 with focused tests that: open the drawer from `dishes/list`, verify `sidebar-drawer-primary-nav` appears, then verify `sidebar-drawer-tools` eventually contains `Đồng bộ dữ liệu mới`, `Sao lưu cá nhân`, `Lịch sử nấu ăn`, `Hướng dẫn sử dụng`, and `Đăng nhập Admin` or the admin unlocked state; click the current-route drawer link and assert `app-route-feedback` is not left visible; navigate through a drawer or bottom-tab route and assert `app-route-feedback` clears after the destination content is visible; and keep global-search and dish/shopping-list detail route flows covered either by this file or by retaining the existing specs unchanged. Keep assertions user-visible or `data-testid` based; do not add visible test-only copy to the app.</action>
  <verify><automated>E2E_BROWSER_CHANNEL=chrome npm run test:e2e -- tests/e2e/app-shell-navigation.spec.ts tests/e2e/global-search.spec.ts tests/e2e/dish-serving-and-modal.spec.ts</automated></verify>
  <acceptance_criteria>
    - `tests/e2e/app-shell-navigation.spec.ts` exists and imports the existing `appTest` fixture or equivalent seeded fixture.
    - The new spec verifies `sidebar-drawer-primary-nav` appears before checking deferred drawer tools.
    - The new spec verifies drawer tools still include sync, Gist backup, cooking history, user guide, and account/admin controls.
    - The new spec verifies `app-route-feedback` does not remain visible after same-route close or completed navigation.
    - The focused Playwright command exits 0 or records exact local browser/server blockers and failing assertions in the summary.
  </acceptance_criteria>
  <done>Automated regression coverage proves Phase 4 did not remove daily drawer/search/detail workflows or leave stale route feedback visible.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 4 evidence and simple visual checks</name>
  <files>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md, docs/large-list-modal-sidebar-performance-note.md</files>
  <read_first>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md, docs/large-list-modal-sidebar-performance-note.md, tests/e2e/performance-regression.spec.ts, .planning/phases/04-navigation-and-app-shell-responsiveness/04-RESEARCH.md, .planning/phases/04-navigation-and-app-shell-responsiveness/04-UI-SPEC.md</read_first>
  <action>Implement D-14, D-16, and D-17 by updating the performance docs with Phase 4 command guidance, budgets, evidence path, and a non-technical visual checklist. In `docs/performance-audit-plan.md`, add a Phase 4 app-shell/navigation section naming `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance`, `perf-10-phase4-app-shell-navigation.json/.md`, drawer shell `1000 ms`, route feedback `1000 ms`, route content-ready `5000 ms`, and 100 ms as warning/ideal evidence. In `docs/automated-regression-test-plan.md`, add a matrix row such as `PERF-SHELL-001` for drawer/navigation/global-search/list-detail app-shell responsiveness. In `docs/large-list-modal-sidebar-performance-note.md`, update the `What You Can Check By Eye` section to say that after Phase 4 the drawer shell should appear quickly with route links first, route feedback should appear immediately, duplicate taps should not stack work, all drawer tools should still be visible after the drawer settles, and existing cooking/shopping/search workflows should still work. Do not add secret values, live GitHub token instructions, or in-app copy changes.</action>
  <verify><automated>rg -n "Phase 4|perf-10-phase4-app-shell-navigation|PERF-SHELL-001|drawer shell|route feedback|What You Can Check By Eye|100 ms" docs/performance-audit-plan.md docs/automated-regression-test-plan.md docs/large-list-modal-sidebar-performance-note.md</automated></verify>
  <acceptance_criteria>
    - `docs/performance-audit-plan.md` contains `perf-10-phase4-app-shell-navigation`.
    - `docs/performance-audit-plan.md` lists drawer shell `1000 ms`, route feedback `1000 ms`, route content-ready `5000 ms`, and `100 ms` warning/ideal target.
    - `docs/automated-regression-test-plan.md` contains `PERF-SHELL-001`.
    - `docs/large-list-modal-sidebar-performance-note.md` contains a simple visual check for drawer route links appearing before drawer tools.
    - The docs grep command exits 0.
  </acceptance_criteria>
  <done>The user and maintainer can understand what got faster, what to check visually, and where the Phase 4 timing evidence lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness to app behavior | Performance tests assert route feedback and drawer tools without adding production diagnostic UI. |
| Documentation to user trust | Docs explain visual checks and evidence without exposing secrets or overstating unresolved timing. |
| Stale overlay to user input | Automated checks must catch overlays that remain visible after navigation settles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-R | Repudiation | Phase 4 performance evidence | mitigate | Write deterministic `perf-10-phase4-app-shell-navigation` JSON and markdown evidence with named interaction ids. |
| T-04-02-D | Denial of Service | Route feedback overlay | mitigate | Regression tests assert same-route and completed navigation do not leave `app-route-feedback` visible. |
| T-04-02-I | Information Disclosure | Docs and tests | mitigate | Do not document `.env` values or secret tokens; keep GitHub/Gist behavior references at workflow/key-name level only. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance` exits 0 or documents exact blockers.
- [ ] `E2E_BROWSER_CHANNEL=chrome npm run test:e2e -- tests/e2e/app-shell-navigation.spec.ts tests/e2e/global-search.spec.ts tests/e2e/dish-serving-and-modal.spec.ts` exits 0 or documents exact blockers.
- [ ] Docs grep command for Phase 4 evidence and visual checks exits 0.
- [ ] Phase 4 summary includes simple user-visible evidence and any remaining timing caveats.
</verification>

<success_criteria>
- Drawer shell, route feedback, bottom tabs, global search navigation, and large-list detail route navigation have separate shell-visible/content-ready timing evidence.
- Automated checks prove deferred drawer tools remain present and route feedback clears after navigation settles.
- User-facing docs explain the improvement in simple visual terms and preserve the known performance evidence path.
</success_criteria>

## Artifacts this phase produces

- New or updated budget object: `phase4Budgets` in `tests/e2e/performance-regression.spec.ts`.
- New performance interaction ids: `phase4-drawer-shell-open`, `phase4-drawer-route-navigation`, `phase4-bottom-tab-navigation`, `phase4-global-search-navigation`, and `phase4-*-detail-route-navigation`.
- New evidence files under `test-results/performance/`: `perf-10-phase4-app-shell-navigation.json` and `.md`.
- New test file: `tests/e2e/app-shell-navigation.spec.ts`.
- New regression matrix row: `PERF-SHELL-001`.
- Updated Phase 4 documentation sections in `docs/performance-audit-plan.md`, `docs/automated-regression-test-plan.md`, and `docs/large-list-modal-sidebar-performance-note.md`.

## Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Navigation timing remains within smoke budget after large-list and online/offline fixes. | Task 1 |
| REQ | LIST-04 | Tasks 1, 2 |
| REQ | UX-02 | Tasks 2, 3 |
| CONTEXT | D-13 through D-17 | Tasks 1, 2, 3 and `must_haves.truths` |
| RESEARCH | App-shell timing matrix, simple visual checklist, drawer/global-search/list-detail verification. | Tasks 1, 2, 3 |
| UI-SPEC | Evidence/user-check contract and no in-app diagnostic text rule. | Task 3 |

<output>
After completion, create `.planning/phases/04-navigation-and-app-shell-responsiveness/04-02-SUMMARY.md`.
</output>
