---
phase: 02-large-list-interaction-hot-paths
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/Dishes/Screens/DishesList.screen.tsx
  - src/Hooks/usePagedVirtualItems.ts
  - tests/e2e/performance-regression.spec.ts
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - TEST-02
must_haves:
  truths:
    - "D-01 and D-03: `dish-search-reset` is the first Phase 2 proof point, using Phase 1 stress evidence of about 13.4s shell-visible and 18.1s content-ready as the before case."
    - "D-05, D-08, and D-15: dish search/reset, row-menu open, and detail/modal open keep separate shell-visible and content-ready timing and show immediate visible feedback before heavier body work."
    - "D-09, D-10, D-11, and D-12: dish rows preserve current image, tags, status, duration, ingredient/step summaries, readiness text, `Nấu`, `Chi tiết`, and menu affordances with exact values only."
    - "D-13, D-14, and D-16: fixed dish hot paths use practical strict smoke budgets while keeping the 100 ms shell-visible target as a warning/ideal."
  artifacts:
    - path: "src/Modules/Dishes/Screens/DishesList.screen.tsx"
      provides: "Dish-first large-list search/reset, row-action, and modal-shell hot-path fix"
      contains: "usePagedVirtualItems"
    - path: "tests/e2e/performance-regression.spec.ts"
      provides: "Focused dish hot-path regression proof or updated strict dish timing assertions"
      contains: "dish-search-reset"
  key_links:
    - from: "tests/e2e/performance-regression.spec.ts"
      to: "src/Modules/Dishes/Screens/DishesList.screen.tsx"
      via: "dish search/reset, row menu, and modal test IDs exercise the optimized list surface"
      pattern: "dish-search-input|dish-row-menu-|dish-readonly-detail-modal"
---

<objective>
Fix the dish list first so the worst measured Phase 1 delay has a concrete before/after result.

Purpose: Phase 1 stress evidence identified `dish-search-reset` as the largest visible freeze. This plan verifies the current partial paging work, removes remaining dish-list render hot-path blockers, and keeps dish row/menu/modal behavior rich and exact.
Output: Dish list interactions respond promptly on large seeded data, with evidence for search/reset, row-menu open, and detail/modal open.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md
@.planning/phases/02-large-list-interaction-hot-paths/02-UI-SPEC.md
@.planning/phases/01-measurement-and-performance-harness/01-VERIFICATION.md
@src/Modules/Dishes/Screens/DishesList.screen.tsx
@src/Hooks/usePagedVirtualItems.ts
@src/Components/Modal/Modal.tsx
@src/Components/List/VirtualListRowFrame.tsx
@tests/e2e/performance-regression.spec.ts
@tests/e2e/performance-baseline.spec.ts
@tests/e2e/fixtures/performanceReport.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the dish hot-path before/after evidence</name>
  <files>tests/e2e/performance-regression.spec.ts, src/Modules/Dishes/Screens/DishesList.screen.tsx</files>
  <read_first>.planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md, .planning/phases/01-measurement-and-performance-harness/01-VERIFICATION.md, tests/e2e/performance-baseline.spec.ts, tests/e2e/performance-regression.spec.ts, src/Modules/Dishes/Screens/DishesList.screen.tsx</read_first>
  <action>Run the Phase 1 stress baseline command with `PERF_DATASET=stress` and `PERF_NETWORK_MODE=online-normal` before code changes if the local browser setup is available. Record `dish-search-reset`, `dish-row-menu-open`, and `dish-detail-modal-open` shell-visible and content-ready values in the plan summary. If the current branch already contains partial Phase 2 paging work and the before value is improved, still use Phase 1 verification as the historical before case and record the current value as the starting value for this plan.</action>
  <verify>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=stress PERF_NETWORK_MODE=online-normal npm run test:e2e:performance:baseline</verify>
  <acceptance_criteria>
    - The plan summary names the historical Phase 1 `dish-search-reset` values: `13432 ms` shell-visible and `18129 ms` content-ready, or cites the exact Phase 1 verification artifact that contains them.
    - If the command runs, the emitted performance evidence contains interaction ids `dish-search-reset`, `dish-row-menu-open`, and `dish-detail-modal-open`.
    - If the command cannot run locally, the summary records the exact command and failure message without blocking source fixes.
  </acceptance_criteria>
  <done>Dish hot-path evidence is anchored before implementation changes are judged.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce dish search/reset render work without dropping row detail</name>
  <files>src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Hooks/usePagedVirtualItems.ts</files>
  <read_first>src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Hooks/usePagedVirtualItems.ts, src/Components/List/VirtualListRowFrame.tsx, .planning/phases/02-large-list-interaction-hot-paths/02-UI-SPEC.md</read_first>
  <action>In `DishesList.screen.tsx`, keep or refine the existing progressive virtual-list path so `filteredDishes` is the complete exact result, `usePagedVirtualItems({ items: filteredDishes, resetKey })` limits the rendered rows, `rowCount` uses `visibleDishes.length`, `rowProps.items` is `visibleDishes`, and `buildDishListSummaries(dishes, visibleDishes)` calculates recursive ingredient and step counts only for visible rows. Keep the small page-status pill copy exactly `Đã tải {loaded}/{total}`. Route search/filter state changes that repopulate the full result through `React.startTransition` or an equivalent React scheduling boundary so input clear and filter taps acknowledge before list repopulation work. Do not remove dish image/fallback, tags, status badge, duration chip, ingredient/step summary blocks, readiness text, `Nấu`, `Chi tiết`, or row-menu controls.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/Modules/Dishes/Screens/DishesList.screen.tsx` contains `usePagedVirtualItems({ items: filteredDishes`.
    - `src/Modules/Dishes/Screens/DishesList.screen.tsx` renders the virtual list with `rowCount={visibleDishes.length}`.
    - `src/Modules/Dishes/Screens/DishesList.screen.tsx` passes `items: visibleDishes` in dish row props.
    - `src/Modules/Dishes/Screens/DishesList.screen.tsx` computes dish summaries from `visibleDishes`, not every filtered dish row.
    - Dish row JSX still contains test or text evidence for image/fallback, ingredient/step counts, `Nấu`, `Chi tiết`, and `dish-row-menu-`.
    - `npm run build` exits 0 or documents an unrelated pre-existing build blocker without new TypeScript errors from this task.
  </acceptance_criteria>
  <done>Dish search/reset no longer forces the full rich row tree and exact recursive summaries into the urgent render path.</done>
</task>

<task type="auto">
  <name>Task 3: Keep dish row menus and modal shells immediate</name>
  <files>src/Modules/Dishes/Screens/DishesList.screen.tsx, tests/e2e/performance-regression.spec.ts</files>
  <read_first>src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Components/Modal/Modal.tsx, tests/e2e/performance-regression.spec.ts, .planning/phases/02-large-list-interaction-hot-paths/02-UI-SPEC.md</read_first>
  <action>Review every dish row action that opens a surface from `DishesItemComponent`: row menu, readonly detail modal, edit modal, cooking modal, and detail-route button. Ensure the overlay shell or route transition starts before expensive modal body work, and keep closed bodies unmounted with `DeferredModalContent active={...}` or existing lazy boundaries. Add or adjust stable nonvisual test hooks only if needed: `dish-row-menu-{id}`, `dish-readonly-detail-modal`, and a content-ready locator inside the readonly detail body. Do not add visible instructional copy, skeleton placeholders, or new UI libraries.</action>
  <verify>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance</verify>
  <acceptance_criteria>
    - Closed dish detail/edit/cooking modal bodies are not mounted before their corresponding open state is true.
    - `DishesList.screen.tsx` keeps `DeferredModalContent` around dish add, readonly detail, edit, and cooking modal bodies where those bodies are present in this screen.
    - The row-menu trigger keeps a stable `data-testid` containing `dish-row-menu-`.
    - The focused performance run records or asserts `daily-dish-row-menu-open`, `daily-dish-detail-modal-open`, and `daily-dish-search-reset`.
  </acceptance_criteria>
  <done>Dish row menus and modal/detail shells provide visible feedback before heavy content work continues.</done>
</task>

<task type="auto">
  <name>Task 4: Prove the dish fix with a practical strict budget</name>
  <files>tests/e2e/performance-regression.spec.ts, src/Modules/Dishes/Screens/DishesList.screen.tsx</files>
  <read_first>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceReport.ts, tests/e2e/performance-baseline.spec.ts, .planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md</read_first>
  <action>Update the focused dish large-list regression path only as much as needed to fail when the fixed dish interactions regress. Keep `strictShellTargetMs` at `100` for warnings. Use practical failure budgets for the fixed dish proof: `dish-search-reset` shell-visible at or below `2500 ms`, `dish-search-reset` content-ready at or below `5000 ms`, `dish-row-menu-open` shell/content at or below `1500 ms`, and `dish-detail-modal-open` shell at or below `1500 ms` with content-ready at or below `5000 ms`. If local evidence shows these budgets are not stable after the code fix, tighten or loosen them only with measured evidence recorded in the summary.</action>
  <verify>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance</verify>
  <acceptance_criteria>
    - `tests/e2e/performance-regression.spec.ts` passes `enforceBudgets: true` for the fixed dish hot-path interactions or otherwise asserts the same practical budgets through `measureInteraction`.
    - The 100 ms shell-visible target remains represented as a warning/ideal, not the practical failure threshold.
    - The evidence written under `test-results/performance/` still includes both `shellVisibleMs` and `contentReadyMs` for dish interactions.
    - If a budget is changed from the values in this task, the summary includes the replacement value and the measured reason.
  </acceptance_criteria>
  <done>Dish fixed hot paths have a focused strict smoke gate without pretending 100 ms is the current fail budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeded browser state to React render path | Performance tests load large Redux Persist data into the browser and exercise list interactions. |
| UI scheduling to user actions | Deferred or transitioned render work must not hide stale or incorrect row values from the user. |
| Test budget configuration to release signal | Strict budgets can create false confidence if they measure the wrong shell/content locator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-T | Tampering | Dish row summaries | mitigate | Keep summary calculations exact and assert visible ingredient/step count behavior through current row locators; do not replace counts with approximations. |
| T-02-01-I | Information Disclosure | Performance evidence | mitigate | Do not print `.env` values or GitHub token values; use only seeded data and test-control env vars. |
| T-02-01-D | Denial of Service | Dish search/reset render path | mitigate | Limit urgent rendering to visible paged rows, defer modal bodies, and enforce practical smoke budgets for fixed interactions. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build` exits 0 or documents only unrelated pre-existing blockers.
- [ ] `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance` exits 0 or documents the exact local browser/server blocker.
- [ ] The summary compares `dish-search-reset` against the Phase 1 13.4s/18.1s stress evidence.
- [ ] Dish row content and Vietnamese user-facing copy are preserved.
</verification>

<success_criteria>
- Dish search/reset no longer creates the measured multi-second pre-open freeze on the available large-list performance run.
- Dish row menu and detail/modal shells appear within the chosen practical smoke budget.
- Dish rows keep exact rich details and stable row spacing while the list progressively repopulates.
</success_criteria>

## Artifacts this phase produces

- No new public module is required for this plan.
- Possible existing-symbol updates: `DishesListScreen`, `buildDishListSummaries`, `DishRow`, `DishesItemComponent`, and `usePagedVirtualItems`.
- Possible test assertion updates in `tests/e2e/performance-regression.spec.ts` for `daily-dish-search-reset`, `daily-dish-row-menu-open`, and `daily-dish-detail-modal-open`.

<output>
After completion, create `.planning/phases/02-large-list-interaction-hot-paths/02-01-SUMMARY.md`.
</output>
