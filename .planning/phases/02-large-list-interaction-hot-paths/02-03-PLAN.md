---
phase: 02-large-list-interaction-hot-paths
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - tests/e2e/fixtures/performanceReport.ts
  - tests/e2e/performance-regression.spec.ts
  - tests/e2e/performance-baseline.spec.ts
  - docs/performance-audit-plan.md
  - docs/automated-regression-test-plan.md
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - TEST-02
must_haves:
  truths:
    - "D-13 and D-16: fixed Phase 2 interactions become strict focused regression gates for search/reset, modal/detail open, and row-menu or row-action open."
    - "D-14: practical failure budgets are used for fixed interactions, while the 100 ms shell-visible target remains a warning/ideal rather than a universal failure threshold."
    - "D-15: every Phase 2 timing record keeps `shellVisibleMs` and `contentReadyMs` separate."
    - "D-01, D-02, and D-03: the final Phase 2 evidence shows dish search/reset first, then ingredient and shopping-list coverage using the same large-list harness."
    - "D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, and D-12: test gates verify list/rendering fixes and row/detail preservation without pulling Phase 3 network/image/sync work or silently dropping rich row content."
  artifacts:
    - path: "tests/e2e/performance-regression.spec.ts"
      provides: "Strict Phase 2 large-list responsiveness gate"
      contains: "phase2"
    - path: "tests/e2e/fixtures/performanceReport.ts"
      provides: "Shared performance evidence fields and warning/failure handling"
      contains: "shellVisibleMs"
    - path: "docs/performance-audit-plan.md"
      provides: "Updated Phase 2 budget and evidence instructions"
      contains: "Phase 2"
  key_links:
    - from: "tests/e2e/performance-regression.spec.ts"
      to: "tests/e2e/fixtures/performanceReport.ts"
      via: "strict large-list gates use shared shell/content measurement"
      pattern: "measureInteraction"
    - from: "docs/automated-regression-test-plan.md"
      to: "tests/e2e/performance-regression.spec.ts"
      via: "docs name the command that enforces Phase 2 hot-path budgets"
      pattern: "test:e2e:performance"
---

<objective>
Turn the fixed large-list interactions into focused strict regression gates with clear evidence.

Purpose: Phase 2 must prove that search/reset, row-menu/action open, and modal/detail open are fixed on the three primary list screens. The gate should fail on practical regressions, preserve 100 ms as an ideal warning, and keep shell-visible/content-ready timing separate.
Output: Updated performance tests and docs that can validate Phase 2 after execution and protect the fixes later.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md
@.planning/phases/02-large-list-interaction-hot-paths/02-UI-SPEC.md
@.planning/phases/02-large-list-interaction-hot-paths/02-01-SUMMARY.md
@.planning/phases/02-large-list-interaction-hot-paths/02-02-SUMMARY.md
@tests/e2e/fixtures/performanceReport.ts
@tests/e2e/performance-regression.spec.ts
@tests/e2e/performance-baseline.spec.ts
@tests/e2e/fixtures/performanceSeed.ts
@tests/e2e/fixtures/seedApp.ts
@docs/performance-audit-plan.md
@docs/automated-regression-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Phase 2 strict budgets in the performance harness</name>
  <files>tests/e2e/fixtures/performanceReport.ts, tests/e2e/performance-regression.spec.ts</files>
  <read_first>tests/e2e/fixtures/performanceReport.ts, tests/e2e/performance-regression.spec.ts, tests/e2e/performance-baseline.spec.ts, .planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md</read_first>
  <action>Keep `measureInteraction` recording `shellVisibleMs`, `contentReadyMs`, `strictShellTargetMs`, and `warnings`. Add a named Phase 2 budget object in `performance-regression.spec.ts` or a small exported helper if the current test structure benefits from reuse. Use these practical failure budgets unless Plan 02-01 or 02-02 summaries provide measured alternatives: search/reset shell-visible `2500 ms`, search/reset content-ready `5000 ms`, row-menu or row-action shell/content `1500 ms`, modal/detail shell `1500 ms`, and modal/detail content-ready `5000 ms`. Keep `strictShellTargetMs: 100` so misses remain warnings in the evidence. Do not change baseline specs to strict failures; baseline remains useful for broader warning/evidence collection.</action>
  <verify>npm run test:e2e:performance -- --list</verify>
  <acceptance_criteria>
    - Phase 2 regression code contains named budget values for search/reset, row-menu or row-action, and modal/detail interactions.
    - Fixed Phase 2 interactions pass `enforceBudgets: true` to `measureInteraction` or use equivalent assertions on `shellVisibleMs` and `contentReadyMs`.
    - `strictShellTargetMs` remains `100` for Phase 2 interactions.
    - Baseline measurement code continues to call `measureInteraction` with `enforceBudgets: false` or an equivalent warning-only path.
    - `npm run test:e2e:performance -- --list` exits 0.
  </acceptance_criteria>
  <done>Phase 2 has explicit strict budgets without losing warning-based 100 ms reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Cover search/reset, modal/detail, and row menu/action across all primary lists</name>
  <files>tests/e2e/performance-regression.spec.ts</files>
  <read_first>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceSeed.ts, tests/e2e/fixtures/seedApp.ts, src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Modules/Ingredient/Screens/IngredientList.screen.tsx, src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx</read_first>
  <action>Extend the focused large-list regression spec so the fixed Phase 2 gate covers the three primary list screens. Include these interaction ids or equivalent stable names: `phase2-dish-search-reset`, `phase2-dish-row-menu-open`, `phase2-dish-detail-modal-open`, `phase2-ingredient-search-reset` or `phase2-ingredient-filter-reset`, `phase2-ingredient-inventory-modal-open`, `phase2-shopping-list-search-reset`, `phase2-shopping-list-row-menu-open`, and `phase2-shopping-list-detail-modal-open` or `phase2-shopping-list-checklist-modal-open`. Use seeded daily data for the strict command to keep the gate reliable, and keep a stress baseline/diagnostic command available for before/after proof. Select row ids from deterministic performance seed data rather than visible text that may change. If a screen has no literal row menu, measure its equivalent high-frequency row action and name it `row-action` in the id.</action>
  <verify>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance</verify>
  <acceptance_criteria>
    - The strict performance spec includes at least one search/reset or filter/reset interaction for dishes, ingredients, and shopping lists.
    - The strict performance spec includes at least one modal/detail open interaction for dishes, ingredients, and shopping lists.
    - The strict performance spec includes a dish row-menu interaction and a shopping-list row-menu interaction, or documents any equivalent row-action fallback in the interaction id.
    - Each Phase 2 interaction record includes both shell-visible and content-ready locators or a documented content-ready fallback equal to the shell locator when the shell is the usable content.
    - The strict daily performance command exits 0 after Phase 2 fixes are in place, or the summary records exact remaining failing interactions and timings.
  </acceptance_criteria>
  <done>All primary large-list screens have strict coverage for the hot paths Phase 2 claims to fix.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve row stability and local-first test isolation in the gate</name>
  <files>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceReport.ts</files>
  <read_first>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceReport.ts, tests/e2e/fixtures/seedApp.ts, tests/e2e/fixtures/performanceNetwork.ts, .planning/phases/02-large-list-interaction-hot-paths/02-UI-SPEC.md</read_first>
  <action>Keep the existing row spacing, drag-as-scroll, closed modal body, and inactive heavy content checks in the regression suite. Add assertions only where needed to protect Phase 2 UI contract: page-status pills have `pointer-events: none`, rows do not collapse or overlap after search/filter reset, and touch drag does not trigger row clicks. Keep GitHub requests stubbed/controlled by the existing seed/network fixtures; do not require live GitHub or `.env` secrets for the Phase 2 gate. Do not make online/offline comparison a strict Phase 2 requirement, because that belongs to Phase 3.</action>
  <verify>E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance</verify>
  <acceptance_criteria>
    - The regression suite still contains a closed modal body or inactive heavy content assertion.
    - The regression suite still contains a row spacing or drag-as-scroll assertion for virtualized lists.
    - The Phase 2 strict gate does not require `PERF_REAL_NETWORK=1`, a GitHub token, or live GitHub requests.
    - No test reads or prints `.env` secret values.
    - Any new page-status assertion checks style/behavior without relying on visible instructional copy.
  </acceptance_criteria>
  <done>The test gate protects row stability and local-first isolation while measuring responsiveness.</done>
</task>

<task type="auto">
  <name>Task 4: Update performance docs with Phase 2 outcomes and commands</name>
  <files>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md</files>
  <read_first>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md, tests/e2e/performance-regression.spec.ts, tests/e2e/performance-baseline.spec.ts, .planning/phases/02-large-list-interaction-hot-paths/02-CONTEXT.md</read_first>
  <action>Update the docs to state that Phase 2 fixed gates cover large-list search/reset, modal/detail open, and row-menu/action open for dishes, ingredients, and shopping lists. Document the strict daily command `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance` and the stress evidence command `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=stress PERF_NETWORK_MODE=online-normal npm run test:e2e:performance:baseline`. List the practical Phase 2 budgets used by the tests and state that the 100 ms shell-visible target remains a warning/ideal. Keep docs clear that online/offline comparison and image/sync/network isolation are Phase 3, not Phase 2.</action>
  <verify>rg -n "Phase 2|test:e2e:performance|2500|5000|100 ms|Phase 3" docs/performance-audit-plan.md docs/automated-regression-test-plan.md</verify>
  <acceptance_criteria>
    - `docs/performance-audit-plan.md` names Phase 2 strict large-list gates.
    - `docs/automated-regression-test-plan.md` contains the strict daily Phase 2 command.
    - Documentation lists separate shell-visible and content-ready budgets or states where to find them in `performance-regression.spec.ts`.
    - Documentation states that 100 ms shell-visible remains a warning/ideal.
    - Documentation keeps online/offline comparison assigned to Phase 3.
  </acceptance_criteria>
  <done>Maintainers know how to run and interpret the Phase 2 responsiveness gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner to release confidence | Strict timing tests influence whether Phase 2 is considered ready to ship. |
| Browser network fixtures to local-first app | Performance runs must remain deterministic without live GitHub or secret-dependent behavior. |
| Documentation to future maintainers | Docs communicate which budgets are strict and which measurements are warnings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-R | Repudiation | Phase 2 evidence | mitigate | Use stable interaction ids and JSON/markdown evidence so failures can be traced to a list and interaction type. |
| T-02-03-I | Information Disclosure | Test commands and docs | mitigate | Document only env key names and test-control variables; do not expose or require secret values. |
| T-02-03-D | Denial of Service | Timing gate instability | mitigate | Use practical budgets, daily strict data, and stress baseline evidence separately so the gate fails meaningful regressions without becoming flaky. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e:performance -- --list` exits 0.
- [ ] `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance` exits 0 or documents the exact local browser/server blocker.
- [ ] Stress evidence is captured with `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=stress PERF_NETWORK_MODE=online-normal npm run test:e2e:performance:baseline` when the local browser setup allows it.
- [ ] Docs name the Phase 2 strict command, practical budgets, and Phase 3 online/offline boundary.
</verification>

<success_criteria>
- Focused large-list responsiveness tests fail when fixed modal/detail, row-menu/action, or search/reset timing exceeds the chosen Phase 2 smoke budgets.
- Evidence continues to report shell-visible and content-ready timings separately.
- Maintainers can explain the Phase 2 outcome in non-subjective terms using test output and docs.
</success_criteria>

## Artifacts this phase produces

- Possible new or revised budget object in `tests/e2e/performance-regression.spec.ts`, such as `phase2Budgets`.
- New or revised interaction ids: `phase2-dish-search-reset`, `phase2-dish-row-menu-open`, `phase2-dish-detail-modal-open`, `phase2-ingredient-search-reset`, `phase2-ingredient-inventory-modal-open`, `phase2-shopping-list-search-reset`, `phase2-shopping-list-row-menu-open`, and `phase2-shopping-list-checklist-modal-open`.
- Updated documentation sections in `docs/performance-audit-plan.md` and `docs/automated-regression-test-plan.md` for Phase 2 commands and budgets.
- No new user-facing UI components, visible copy, or third-party UI assets are expected in this plan.

<output>
After completion, create `.planning/phases/02-large-list-interaction-hot-paths/02-03-SUMMARY.md`.
</output>
