---
phase: 03-online-and-offline-cost-isolation
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - package.json
  - tests/e2e/runPerformanceCommand.cjs
  - tests/e2e/performance-regression.spec.ts
  - tests/e2e/fixtures/performanceNetwork.ts
  - tests/e2e/fixtures/performanceReport.ts
  - docs/performance-audit-plan.md
  - docs/automated-regression-test-plan.md
autonomous: true
requirements:
  - PERF-03
  - NET-01
  - NET-02
  - NET-03
  - NET-04
  - TEST-03
must_haves:
  truths:
    - "D-13 and TEST-03: maintainers can run deterministic online-normal, browser-offline, and slow/blocked GitHub/image comparison checks without live GitHub."
    - "D-14: service-worker production behavior remains optional diagnostic evidence outside the strict Phase 3 gate."
    - "D-15: comparison checks reuse Phase 2 hot paths on dish, ingredient, and shopping-list large screens."
    - "D-16: strict failures stay no worse than practical Phase 2 budgets while 100 ms shell-visible remains warning evidence."
  artifacts:
    - path: "package.json"
      provides: "Repo-local Phase 3 comparison command"
      contains: "test:e2e:performance:phase3"
    - path: "tests/e2e/performance-regression.spec.ts"
      provides: "Online/offline comparison gate"
      contains: "phase3-comparison"
    - path: "docs/performance-audit-plan.md"
      provides: "Phase 3 evidence and optional service-worker diagnostic guidance"
      contains: "Phase 3"
  key_links:
    - from: "package.json"
      to: "tests/e2e/runPerformanceCommand.cjs"
      via: "package script invokes the Phase 3 comparison runner"
      pattern: "test:e2e:performance:phase3"
    - from: "tests/e2e/runPerformanceCommand.cjs"
      to: "tests/e2e/performance-regression.spec.ts"
      via: "runner sets comparison env and launches the regression spec"
      pattern: "PERF_PHASE3_COMPARE"
    - from: "tests/e2e/performance-regression.spec.ts"
      to: "tests/e2e/fixtures/performanceReport.ts"
      via: "comparison writes JSON and markdown evidence with network/image/resource diagnostics"
      pattern: "writePerformanceEvidence"
---

<objective>
As a maintainer, I want one repeatable Phase 3 comparison gate so that I can see whether online sync, offline mode, GitHub delay, image delay, or unrelated list rendering changes affect responsiveness.

Purpose: Phase 3 is complete only when the online/offline issue is measurable after the sync and image fixes. This plan turns the behavior into a repo-local gate and updates docs so later rich feature, UI, UX, and performance work can protect the result.
Output: A Phase 3 comparison command, online/offline/slow-network evidence files, resource diagnostics, and documentation separating strict checks from optional service-worker diagnostics.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-online-and-offline-cost-isolation/03-CONTEXT.md
@.planning/phases/03-online-and-offline-cost-isolation/03-RESEARCH.md
@.planning/phases/03-online-and-offline-cost-isolation/03-VALIDATION.md
@.planning/phases/03-online-and-offline-cost-isolation/03-UI-SPEC.md
@.planning/phases/03-online-and-offline-cost-isolation/03-PATTERNS.md
@.planning/phases/03-online-and-offline-cost-isolation/03-01-SUMMARY.md
@.planning/phases/03-online-and-offline-cost-isolation/03-02-SUMMARY.md
@.planning/phases/02-large-list-interaction-hot-paths/02-03-SUMMARY.md
@package.json
@tests/e2e/runPerformanceCommand.cjs
@tests/e2e/performance-regression.spec.ts
@tests/e2e/fixtures/performanceNetwork.ts
@tests/e2e/fixtures/performanceReport.ts
@docs/performance-audit-plan.md
@docs/automated-regression-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Phase 3 comparison runner and strict-mode switch</name>
  <files>package.json, tests/e2e/runPerformanceCommand.cjs, tests/e2e/performance-regression.spec.ts</files>
  <read_first>package.json, tests/e2e/runPerformanceCommand.cjs, tests/e2e/performance-regression.spec.ts, tests/e2e/performance-baseline.spec.ts, .planning/phases/03-online-and-offline-cost-isolation/03-PATTERNS.md</read_first>
  <action>Add a repo-local package script named `test:e2e:performance:phase3` that invokes `node tests/e2e/runPerformanceCommand.cjs phase3`. Extend `runPerformanceCommand.cjs` so mode `phase3` sets `PERF_PHASE3_COMPARE=1`, defaults `PERF_DATASET` to `daily` when not provided, defaults `PERF_NETWORK_MODE` to `online-normal,browser-offline,mocked-slow-network`, and launches `@playwright/test` against `tests/e2e/performance-regression.spec.ts` with inherited extra Playwright args. Keep existing `baseline` and `diagnostic` behavior unchanged. In `performance-regression.spec.ts`, add a `PERF_PHASE3_COMPARE` branch or describe block tagged with `phase3-comparison` that parses comma-separated `PERF_NETWORK_MODE` values and maps image modes to `fast` for `online-normal`, `blocked` for `browser-offline`, and `slow` for `mocked-slow-network` unless `PERF_IMAGE_MODE` overrides the mapping. Reuse the Phase 2 `phase2Budgets` values and 100 ms warning target per D-16.</action>
  <verify><automated>npm run test:e2e:performance:phase3 -- --list</automated></verify>
  <acceptance_criteria>
    - `package.json` contains `test:e2e:performance:phase3`.
    - `runPerformanceCommand.cjs` accepts `phase3` without changing `baseline` or `diagnostic` modes.
    - `phase3` mode sets `PERF_PHASE3_COMPARE=1` for the spawned Playwright process.
    - The Phase 3 comparison branch can list tests with `npm run test:e2e:performance:phase3 -- --list`.
    - The comparison branch uses Phase 2 practical budgets and `strictShellTargetMs: 100`.
  </acceptance_criteria>
  <done>Maintainers have a dedicated command surface for Phase 3 online/offline comparison checks.</done>
</task>

<task type="auto">
  <name>Task 2: Write comparison evidence across online, offline, GitHub, and image modes</name>
  <files>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceNetwork.ts, tests/e2e/fixtures/performanceReport.ts</files>
  <read_first>tests/e2e/performance-regression.spec.ts, tests/e2e/fixtures/performanceNetwork.ts, tests/e2e/fixtures/performanceReport.ts, tests/e2e/fixtures/seedApp.ts, .planning/phases/03-online-and-offline-cost-isolation/03-01-SUMMARY.md, .planning/phases/03-online-and-offline-cost-isolation/03-02-SUMMARY.md</read_first>
  <action>Implement D-13 and D-15 by making the `phase3-comparison` tests run the same large-list interactions across `online-normal`, `browser-offline`, and `mocked-slow-network`: dish search reset, dish row menu or detail modal open, ingredient search reset or inventory modal open, shopping-list search reset, and shopping-list row menu or checklist modal open. Use the deterministic daily dataset and the sync freshness/network fixtures from Plan 03-01 so GitHub requests are due and controlled. For `mocked-slow-network`, set `githubDelayMs` and slow image mode so the evidence can distinguish GitHub and image costs. Extend `performanceReport.ts` only if needed to include clear diagnostics such as `githubRequestCount`, `sharedManifestRequestCount`, `sharedDataRequestCount`, `imageRequestCount`, `blockedImageRequestCount`, and `delayedImageRequestCount`; otherwise put those fields in `diagnostics`. Write evidence files under `test-results/performance/` with file names like `perf-08-phase3-daily-online-normal`, `perf-08-phase3-daily-browser-offline`, and `perf-08-phase3-daily-mocked-slow-network`. Use Phase 2 practical failure budgets for strict assertions and keep 100 ms as warning evidence.</action>
  <verify><automated>E2E_BROWSER_CHANNEL=chrome npm run test:e2e:performance:phase3</automated></verify>
  <acceptance_criteria>
    - Phase 3 comparison evidence covers `online-normal`, `browser-offline`, and `mocked-slow-network` in one command when no narrower env override is provided.
    - Evidence JSON includes `networkMode`, `imageMode`, `interactions`, `warnings`, `resources`, and diagnostics that identify GitHub and image request behavior.
    - The compared interactions include at least one dish, one ingredient, and one shopping-list large-screen hot path.
    - Strict assertions use no looser budgets than Phase 2 practical budgets documented in `phase2Budgets`.
    - The Phase 3 comparison command exits 0 or records exact failing mode, interaction id, and timing/resource diagnostics in the summary.
  </acceptance_criteria>
  <done>Online/offline/network/image cost differences are visible in deterministic JSON and markdown evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 3 gates and optional service-worker diagnostics</name>
  <files>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md</files>
  <read_first>docs/performance-audit-plan.md, docs/automated-regression-test-plan.md, tests/e2e/runPerformanceCommand.cjs, tests/e2e/performance-regression.spec.ts, .planning/phases/03-online-and-offline-cost-isolation/03-CONTEXT.md</read_first>
  <action>Update `docs/performance-audit-plan.md` and `docs/automated-regression-test-plan.md` with the strict Phase 3 command `E2E_BROWSER_CHANNEL=chrome npm run test:e2e:performance:phase3`, the default comparison matrix `online-normal`, `browser-offline`, and `mocked-slow-network`, and the evidence file naming pattern under `test-results/performance/`. Document that D-16 keeps search/reset `2500/5000 ms`, row menu/action `3500/3500 ms`, and modal/detail `2000/5000 ms` as strict practical budgets while 100 ms shell-visible remains warning evidence. Document D-14 by keeping production service-worker behavior as optional diagnostic evidence only, with a clearly separate command or note that normal strict checks unregister service workers through `seedApp`. Do not add in-app explanatory copy about performance, sync scheduling, service workers, or test modes.</action>
  <verify><automated>rg -n "Phase 3|test:e2e:performance:phase3|online-normal|browser-offline|mocked-slow-network|100 ms|service-worker" docs/performance-audit-plan.md docs/automated-regression-test-plan.md</automated></verify>
  <acceptance_criteria>
    - `docs/performance-audit-plan.md` contains `test:e2e:performance:phase3`.
    - `docs/automated-regression-test-plan.md` contains `test:e2e:performance:phase3`.
    - Docs list the three strict comparison modes: `online-normal`, `browser-offline`, and `mocked-slow-network`.
    - Docs state that 100 ms shell-visible remains a warning/ideal for Phase 3.
    - Docs state that service-worker production behavior is optional diagnostic evidence, not the strict Phase 3 gate.
  </acceptance_criteria>
  <done>Maintainers can run, interpret, and preserve the Phase 3 comparison gate without confusing it with optional service-worker diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package script to release confidence | The Phase 3 command determines whether online/offline responsiveness is accepted. |
| Browser network routing to evidence | Controlled routes must distinguish GitHub, shared data, and image behavior. |
| Documentation to future maintenance | Docs must keep strict gates and optional diagnostics separate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-R | Repudiation | Phase 3 comparison evidence | mitigate | Use deterministic file names, mode metadata, interaction ids, and diagnostics. |
| T-03-03-I | Information Disclosure | Test command/docs | mitigate | Keep GitHub live network opt-in and never document secret values. |
| T-03-03-D | Denial of Service | Strict comparison gate | mitigate | Use daily dataset, practical Phase 2 budgets, and service-worker-free strict checks. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e:performance:phase3 -- --list` exits 0.
- [ ] `E2E_BROWSER_CHANNEL=chrome npm run test:e2e:performance:phase3` exits 0 or documents exact local browser/server blockers.
- [ ] `E2E_BROWSER_CHANNEL=chrome PERF_DATASET=daily PERF_NETWORK_MODE=online-normal npm run test:e2e:performance` still exits 0 or documents exact blockers.
- [ ] Docs name the Phase 3 command, comparison modes, practical budgets, and optional service-worker diagnostic boundary.
</verification>

<success_criteria>
- Maintainer can compare online-normal, browser-offline, and mocked slow GitHub/image modes from a repo-local command.
- Evidence identifies whether responsiveness cost likely comes from sync, GitHub request delay, image request/decode work, service-worker diagnostics, or an unrelated list regression.
- The Phase 2 daily large-list gate remains preserved after Phase 3 changes.
</success_criteria>

## Artifacts this phase produces

- New package script: `test:e2e:performance:phase3`.
- New runner mode: `phase3` in `tests/e2e/runPerformanceCommand.cjs`.
- New environment flag: `PERF_PHASE3_COMPARE=1`.
- New comparison test branch or tag: `phase3-comparison`.
- New evidence files under `test-results/performance/`, for example `perf-08-phase3-daily-online-normal.json`, `perf-08-phase3-daily-browser-offline.json`, and `perf-08-phase3-daily-mocked-slow-network.json`.
- Updated Phase 3 documentation sections in `docs/performance-audit-plan.md` and `docs/automated-regression-test-plan.md`.

<output>
After completion, create `.planning/phases/03-online-and-offline-cost-isolation/03-03-SUMMARY.md`.
</output>
