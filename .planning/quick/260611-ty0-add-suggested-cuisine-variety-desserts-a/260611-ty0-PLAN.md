---
quick_id: 260611-ty0
status: in_progress
created: 2026-06-11
task: Add suggested cuisine variety desserts and dish image URLs
---

# Quick Task 260611-ty0: Add Suggested Cuisine Variety, Desserts, and Dish Images

## Goal

Add the suggested cuisine-variety dishes plus a stronger dessert/drink set. Every new dish should include an image URL, and ingredient combinations should be checked to avoid misleading recipes.

## Scope

- Add Korean, Japanese, Thai, Western/Italian, health-friendly, dessert, and drink dishes.
- Add only missing ingredients required by those dishes.
- Include image URLs for every newly-added dish.
- Regenerate split shared sync data and legacy shared-data mirrors.
- Update shared manifests with accurate counts, versions, changes, and stable hashes.

## Verification

- Parse all touched JSON files.
- Check duplicate ingredient and dish IDs/names.
- Check supported units only.
- Check every new dish has an image URL.
- Check every dish ingredient reference resolves to an existing ingredient.
- Check manifest counts and stable SHA-256 hashes.
- Check legacy `docs/shared-data.json` entries match split sync entries by ID.
- Run `git diff --check`.
