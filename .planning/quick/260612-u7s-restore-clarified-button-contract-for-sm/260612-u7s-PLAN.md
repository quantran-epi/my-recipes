# Quick Task 260612-u7s: Restore clarified button contract for Smart Planner and Dish Suggester

## Goal

Apply the clarified button rules: normal modal buttons stay in the normal Button family, ActionButton stays compact for inline actions, and the Dish Suggester overflow trigger matches the normal icon-button height.

## Tasks

1. Restore ActionButton behavior
   - Return ActionButton to compact pill defaults.
   - Keep circular help icon support through the explicit `shape='circle'` path.

2. Fix requested screen regressions
   - Change the Smart Planner create scheduled meal modal cancel action from ActionButton to normal Button.
   - Resize the Dish Suggester bottom overflow trigger to the normalized square normal icon-button style.

3. Verify and commit
   - Run `npx tsc --noEmit`.
   - Commit only the focused source/docs/tracking changes.
