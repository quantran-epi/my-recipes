# Quick Task 260608-v6r: Fullscreen Onboarding and Realistic Fake Tour

## Goal

Replace the generic guide tour with a polished full-screen onboarding carousel and a robust interactive tour that mirrors the real app pages using fake guide-only data.

## Tasks

1. Move welcome and tour routes outside the normal app shell so they can render full screen without the production header/bottom tabs.
2. Redesign the welcome flow as a full-screen slide carousel with large visual panels, fixed previous/next arrow controls, and bottom dot pagination.
3. Replace Ant Design Tour usage with a custom spotlight system to avoid overflow, bad popup placement, and overlap on mobile.
4. Render realistic fake versions of core daily pages: dashboard, inventory, dishes, dish suggestions, shopping list, and scheduled meal.
5. Keep real feature pages untouched; only the app shell keeps the one-time welcome redirect flag check.
6. Build and deploy updated static output to `docs/`.

## Verification

- `yarn build`
- `git diff --check`
- Confirm `docs/manifest.json` is not overwritten during deployment.
