---
phase: quick-260613-f6m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/Dishes/Screens/CookingSession.widget.tsx
  - src/Store/Models/SmartPlannerTemplate.ts
  - src/Store/Reducers/SmartPlannerTemplateReducer.ts
  - src/Store/Store.ts
  - src/Store/Selectors.ts
  - src/Modules/ScheduledMeal/Screens/SmartMealPlanner.screen.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Starting a dish from the scheduled-meal path shows the live countdown timer card when the dish has duration phases (or the audio alarm works there) — matching the direct cooking path"
    - "In the cooking view where the user marks ingredients used, each ingredient shows its availability (have vs lacking)"
    - "An ingredient without enough stock genuinely cannot be marked used"
    - "User can save the current per-slot dish-count ranges + tag requirements as a named template that survives reload"
    - "User can pick a saved template inside the dishes-per-meal modal and have it populate the slot ranges + tag requirements"
  artifacts:
    - path: "src/Store/Models/SmartPlannerTemplate.ts"
      provides: "SmartPlannerTemplate type"
      contains: "SmartPlannerTemplate"
    - path: "src/Store/Reducers/SmartPlannerTemplateReducer.ts"
      provides: "Template slice (add/remove) under personalReducer"
      contains: "createSlice"
    - path: "src/Modules/Dishes/Screens/CookingSession.widget.tsx"
      provides: "Scheduled-path timer fallback + ingredient availability/guard fixes"
  key_links:
    - from: "src/Store/Store.ts"
      to: "SmartPlannerTemplateReducer"
      via: "personalReducer registration"
      pattern: "smartPlannerTemplate"
    - from: "src/Modules/ScheduledMeal/Screens/SmartMealPlanner.screen.tsx"
      to: "selectSmartPlannerTemplates"
      via: "useSelector + dispatch save/apply"
      pattern: "SmartPlannerTemplate"
---

<objective>
Fix three independent issues in one quick pass:
1. Cooking session countdown does not appear/ring when a dish is started from the scheduled-meal path.
2. Ingredient availability is not visible everywhere the user marks ingredients used, and insufficient ingredients may still be markable.
3. Smart planner per-slot dish-count + tag requirements cannot be saved as reusable templates.

Purpose: Close real user-reported bugs in cooking flow and add a quality-of-life template feature to the planner.
Output: Diagnosed + fixed CookingSession widget, a new persisted SmartPlannerTemplate slice, and template save/apply UI in the dishes-per-meal modal.

NOTE on uncertainty: Areas 1 and 2 have UNCERTAIN root causes and possible pre-existing partial fixes. Each fix task MUST begin by reproducing/reading the current behavior and confirming what is actually broken BEFORE editing. Do not write speculative fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/Modules/Dishes/Screens/CookingSession.widget.tsx
@src/Modules/Dishes/Screens/useCookingTimer.ts
@src/Modules/ScheduledMeal/Screens/ScheduledMealCooking.widget.tsx
@src/Modules/ScheduledMeal/Screens/SmartMealPlanner.screen.tsx
@src/Store/Store.ts
@src/Store/Selectors.ts
@src/Store/Reducers/HouseholdHealthReducer.ts

Verification note: Jest currently fails resolving the `@store/Store` alias and Playwright needs an unsandboxed server, so the automated gate for every task is `node_modules/.bin/tsc --noEmit -p tsconfig.json` plus a grep for the key symbol. Manual smoke steps live in <verification>. Commit each task separately.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix scheduled-meal cooking countdown</name>
  <files>src/Modules/Dishes/Screens/CookingSession.widget.tsx</files>
  <action>
FIRST diagnose, do not assume. Read the mount/phase-gate logic in CookingSessionWidget: default state `phase="prep"`, `activeSession = sessions.find(s => s.dishId === dish.id && s.status === "cooking")`, and the render gate `if ((phase === "cooking" || activeSession) && session)`. On the scheduled-meal path, ScheduledMealCookingModal's `_startDish` dispatches `startCooking` with `timerPhases` BEFORE mounting CookingSessionWidget, so the widget enters the cooking view via the `activeSession` branch and `_onStartCooking` (which calls `timerView.unlockAudio()`) never runs.

Determine which of two failures is real:
  (a) The visual `CookingTimerCard` does not render at all on this path — check `timerView.hasTimer` (true when `session.timer` exists) and the render condition `timerView.hasTimer && !timerView.isFinished`. If `session.timer` is populated (it should be, since `timerPhases` reaches the reducer), the card should render and (a) is NOT the bug.
  (b) Only the AUDIO alarm is broken because `timerView.unlockAudio()` was never called inside a user gesture on this path.
Also confirm the test dishes actually have duration phases: if `DishDurationHelper.getActiveItems(dish.duration)` is empty there is no timer by design — that is NOT a bug; note it in the summary and stop.

Once the real cause is confirmed, apply the minimal fix. The expected robust fix (if cause is (b), or the card renders but audio is silent): add a `useEffect` in CookingSessionWidget that fires once when the widget enters the cooking view via `activeSession` rather than via `_onStartCooking` — i.e. when `(phase !== "cooking") && activeSession && session` and the session has timer phases — and calls `timerView.unlockAudio()` as a fallback. Guard it so it runs at most once per session (e.g. track the session id in a ref) and does not double-fire on the direct path where `_onStartCooking` already unlocked audio. If diagnosis shows cause (a) (card genuinely not rendering), fix the actual render/gate condition instead and explain why in the summary. Do not introduce per-second Redux writes; the live countdown stays computed in `useCookingTimer`.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n "unlockAudio" src/Modules/Dishes/Screens/CookingSession.widget.tsx</automated>
  </verify>
  <done>tsc passes; CookingSession.widget.tsx contains a fallback that ensures audio unlock (or the corrected render path) when the cooking view is entered via activeSession. Summary states which root cause was confirmed and why.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnose and fix ingredient availability visibility + insufficient-used guard</name>
  <files>src/Modules/Dishes/Screens/CookingSession.widget.tsx</files>
  <action>
FIRST diagnose, do not assume — a partial fix already appears present. Read `IngredientChecklist` (rendered twice: prep view `interactive={false}`, active cooking view `interactive`). Confirm by reading the current code:
  1. Availability tags: `_renderInventoryAvailability(row)` is rendered for each row, but in the `interactive={false}` prep instance availability is shown via the trailing `<Space>` branch while in the `interactive` instance it is shown via the inline block under the name. Verify both views actually surface availability for the user; if the prep view path omits it for any row state, that is gap (1).
  2. The used-toggle guard: `disableUsedToggle = interactive && !row.sufficient && status !== "used"` drives `Switch disabled`, and `onChange` also guards `if (checked && !row.sufficient) return;`. Verify the guard is genuinely effective and not bypassable. Check the edge where `status === "used"` re-enables the switch (allowing an already-used insufficient ingredient to stay used) and the `alwaysAvailable` interplay (alwaysAvailable ingredients are sufficient by stock math, so they should remain markable — confirm they are not wrongly blocked).

Then fix only the confirmed gaps:
  - Ensure availability (have vs lacking) is visible in the cooking/interactive view right where the user toggles "used" (the primary place the user marks usage). If it is missing or unclear, surface it.
  - Ensure an ingredient without enough stock genuinely cannot be marked used. Keep the disable + onChange guard, and add a brief user-facing reason for WHY the toggle is disabled (e.g. a small "Chưa đủ" hint near the switch or tooltip) so the disabled state is not silently confusing.
Keep changes minimal and consistent with existing Vietnamese copy and Tag styling. Do not alter the inventory math in `rows` useMemo.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n "disableUsedToggle\|_renderInventoryAvailability" src/Modules/Dishes/Screens/CookingSession.widget.tsx</automated>
  </verify>
  <done>tsc passes; availability is visible where the user marks ingredients used, the insufficient-used guard is confirmed effective (including the status==="used" edge), and a reason for the disabled toggle is shown. Summary states which gaps were real vs already-handled.</done>
</task>

<task type="auto">
  <name>Task 3: Add persisted SmartPlannerTemplate slice + selector</name>
  <files>src/Store/Models/SmartPlannerTemplate.ts, src/Store/Reducers/SmartPlannerTemplateReducer.ts, src/Store/Store.ts, src/Store/Selectors.ts</files>
  <action>
Create the minimum-viable template model and slice. The template captures exactly the two modal configs.

1. `src/Store/Models/SmartPlannerTemplate.ts`: export `type SmartPlannerTemplate = { id: string; name: string; createdAt: string; mealSlotDishRanges: SmartPlannerMealSlotDishRanges; mealSlotTagRequirements: SmartPlannerMealSlotTagRequirements; }`. Import the two config types from `@modules/ScheduledMeal/Helpers/SmartPlannerEngine` (re-export them or import-and-reference). Also export `interface SmartPlannerTemplateState { templates: SmartPlannerTemplate[] }`.

2. `src/Store/Reducers/SmartPlannerTemplateReducer.ts`: follow the `createSlice` pattern used in HouseholdHealthReducer.ts (initialState tolerant of absent persisted state, normalize on read where helpful). Initial state `{ templates: [] }`. Reducers: `addSmartPlannerTemplate` (PayloadAction<SmartPlannerTemplate>; push to front, cap the list at a sane max e.g. 30) and `removeSmartPlannerTemplate` (PayloadAction<{ id: string }>; filter out). Export the actions and `export default slice.reducer`.

3. `src/Store/Store.ts`: register the reducer under `personalReducer` as `smartPlannerTemplate: SmartPlannerTemplateReducer`. No persist-config change is needed — `personalPersistConfig` (IndexedDB) auto-persists the new key. This is additive; absent persisted state must not crash (initialState covers it).

4. `src/Store/Selectors.ts`: add `export const selectSmartPlannerTemplates = (state: RootState) => state.personal.smartPlannerTemplate?.templates ?? [];` in the Personal section (the `?? []` tolerates devices whose persisted blob predates this slice).
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n "smartPlannerTemplate" src/Store/Store.ts && grep -n "selectSmartPlannerTemplates" src/Store/Selectors.ts</automated>
  </verify>
  <done>tsc passes; new slice registered under personalReducer; `selectSmartPlannerTemplates` exported with absent-state fallback; add/remove actions exported.</done>
</task>

<task type="auto">
  <name>Task 4: Wire template save + apply into the dishes-per-meal modal</name>
  <files>src/Modules/ScheduledMeal/Screens/SmartMealPlanner.screen.tsx</files>
  <action>
Add "Save as template" and a template picker ("quick apply") inside `mealRangeModal` (the modal opened by `_suggestMeals` for day/week scope), using the slice from Task 3.

1. Read templates with `useSelector(selectSmartPlannerTemplates)` and get `dispatch`.
2. Apply: render a compact picker (e.g. a Select or a row of preset buttons consistent with existing `.smart-planner-preset` styling) listing saved templates by name. On choose, call `setMealSlotDishRanges(template.mealSlotDishRanges)` and `setMealSlotTagRequirements(template.mealSlotTagRequirements)`, then `_clearSuggestions()`. Guard against stale tags: when applying, drop any tag requirement whose tag is no longer in `tagRequirementOptions` (current dish tags) so an applied template never references deleted tags.
3. Save: add a "Lưu thành mẫu" action (button) that prompts for a name. Reuse the existing `modal` (useModal) or a small inline Input + confirm consistent with the codebase; on confirm dispatch `addSmartPlannerTemplate({ id: nanoid(10), name: trimmedName, createdAt: new Date().toISOString(), mealSlotDishRanges, mealSlotTagRequirements })` and `message.success(...)`. Reject empty names.
4. Allow removing a saved template (e.g. a small delete affordance on each picker entry) dispatching `removeSmartPlannerTemplate({ id })`.
Keep all new copy in Vietnamese matching surrounding text. Do not change the suggestion engine call or the existing min/max/tag stepper controls — only add save/apply/remove around them.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n "addSmartPlannerTemplate\|selectSmartPlannerTemplates" src/Modules/ScheduledMeal/Screens/SmartMealPlanner.screen.tsx</automated>
  </verify>
  <done>tsc passes; mealRangeModal lets the user save the current ranges+tag requirements as a named template, pick a saved template to populate both configs (stale tags filtered), and remove templates. Templates persist across reload.</done>
</task>

</tasks>

<verification>
Automated gate per task: `node_modules/.bin/tsc --noEmit -p tsconfig.json` plus the per-task grep. Jest and Playwright are currently blocked (see context), so functional confirmation is manual.

Manual smoke (after all tasks, dev server in an unsandboxed shell):
1. Timer: Open a scheduled meal whose dish has configured duration phases, start it from the scheduled-meal modal. Confirm the countdown card renders and that, after the first phase elapses, the alarm rings (audio was unlocked). Compare against starting the same dish from the direct cooking path. If the dish has no duration phases, confirm no timer is expected.
2. Ingredients: In the cooking view, confirm each ingredient shows availability (đủ / còn X / chưa có) next to the used toggle. For an ingredient with insufficient stock, confirm the "used" toggle is disabled with a visible reason and cannot be switched on. Confirm alwaysAvailable ingredients remain markable.
3. Template: In day/week scope, open the dishes-per-meal modal, set some ranges + tag requirements, save as a template with a name. Reload the app, reopen the modal, pick the template, confirm ranges + tag requirements populate. Delete a template and confirm it is gone after reload.
</verification>

<success_criteria>
- All four tasks pass `tsc --noEmit` and their grep checks.
- Scheduled-meal cooking surfaces the countdown/alarm consistent with the direct path (or it is confirmed there are simply no duration phases).
- Ingredient availability is visible where usage is marked, and insufficient ingredients cannot be marked used (with a reason shown).
- Smart planner templates can be saved, applied (stale tags filtered), and removed, and persist across reload via the personal IndexedDB root.
- Each task committed separately.
</success_criteria>

<output>
Create `.planning/quick/260613-f6m-cooking-session-countdown-ingredient-ava/260613-f6m-SUMMARY.md` when done.
</output>
