# Quick Task 260608-s7j: Mobile Scheduled Meal And Nutrition Suggester Fixes

## Goal

Fix the mobile scheduled meal date picker button wrapping, align the nutrition dish suggestor expander with the fridge dish suggestor plus/minus UI, remove hover tooltip wrappers from the dish suggestor actions, then deploy the static app.

## Tasks

1. Update scheduled meal date controls so the calendar chooser is a stable icon-only button that cannot wrap on mobile.
2. Update nutrition dish suggestion rows so the details expander uses the same compact plus/minus affordance as the fridge suggestor.
3. Remove dish suggestor hover tooltip wrappers around icon actions, build, copy deployment output into `docs/`, commit, and push.

## Verification

- `yarn build`
- Confirm no `Tooltip` import remains in `DishSuggester.screen.tsx`.
- Confirm the deployed `docs/` output is refreshed without overwriting `docs/manifest.json`.
