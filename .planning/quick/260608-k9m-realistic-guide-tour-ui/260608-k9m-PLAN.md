# Quick Task 260608-k9m: Realistic Guide Tour UI

## Goal

Refine the onboarding and tour so the guide screens closely resemble the real app UI while still using fake guide-only data.

## Tasks

1. Update welcome carousel slides to preview realistic parts of the app UI and use shorter, friendlier explanation text.
2. Enrich tour fake data and adjust fake screens to match real page structure, spacing, header cards, filters, rows, action areas, and daily workflows more closely.
3. Add a User Guide page trigger to replay the welcome onboarding at any time.
4. Keep real feature pages untouched and keep the tour isolated to guide screens.
5. Build, verify, deploy to `docs/`, commit, and push.

## Verification

- `yarn build`
- `git diff --check`
- Mobile layout sanity check for welcome and tour routes.
- Confirm `docs/manifest.json` is not overwritten.
