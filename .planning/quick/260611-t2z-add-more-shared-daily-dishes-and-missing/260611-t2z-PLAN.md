---
quick_id: 260611-t2z
status: in_progress
created: 2026-06-11
task: Add more shared daily dishes and missing ingredients
---

# Quick Task 260611-t2z: Add More Shared Daily Dishes and Missing Ingredients

## Goal

Expand the shared cookbook data with more everyday dishes, adding only the missing ingredients needed by those dishes. Keep the split shared-data sync files and legacy shared-data files consistent so user sync detects the new data correctly.

## Scope

- Add missing shared ingredients for vegetables, fish, condiments, pantry breakfast items, and wrappers used by the new dishes.
- Add a larger set of common household dishes using existing and newly-added ingredients.
- Regenerate shared manifests with accurate counts, versions, hashes, and change lists.
- Validate JSON integrity, dish ingredient references, manifest hashes, and legacy/split consistency.

## Out of Scope

- App code, UI, reducers, selectors, or deployment output unrelated to shared data.
- Existing ingredient or dish rewrites unless required for consistency.

## Verification

- Parse all touched JSON files.
- Check no duplicate ingredient or dish IDs.
- Check every dish ingredient reference exists.
- Check split manifest counts and hashes match actual stable JSON.
- Check legacy `docs/shared-data.json` contains the same entries by ID as split sync files.
- Run `git diff --check`.
