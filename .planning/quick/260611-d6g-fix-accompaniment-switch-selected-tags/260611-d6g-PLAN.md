# Quick Task 260611-d6g: Fix accompaniment persistence and selected tag visibility

## Scope

Address the two follow-up issues reported after the previous deploy:

1. Dish add/edit must reliably save `isAccompaniment` from the Switch control.
2. Multi/tag select dropdowns must show an added section listing all currently selected items when the control is opened.

## Tasks

1. Patch dish add/edit form defaults and Switch binding so submitted values always include the current `isAccompaniment` value.
2. Extend the shared select wrapper with a compact selected-items section in multi/tag dropdowns while preserving existing option menus and hidden-tag placeholder behavior.
3. Apply the selected-items dropdown behavior to direct Ant Design multi/tag selects that do not use the shared wrapper.
4. Build and deploy according to `docs/deployment.md`.

## Verification

- Run `yarn build`.
- Copy `build/` output to `docs/` excluding `manifest.json`.
- Stage, commit, and push source plus docs deployment output.
