---
quick_id: 260607-v9x
status: planned
created: 2026-06-07
---

# Quick Task 260607-v9x: Scheduled Meal Day-Focused UI

## Goal

Update the Scheduled Meal page so the first view focuses on today's schedule, keeps the calendar hidden by default, adds clear previous/next day navigation, uses shorter top action labels, and gives scheduled meal cards a more focused dashboard-style header.

## Tasks

1. Update `src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx` day navigation and calendar visibility.
2. Restyle the top actions and scheduled meal item chrome while preserving existing meal rows and modals.
3. Build, deploy to `docs/`, commit, and push.

## Verification

- `yarn build`
- Confirm deploy output is copied from `build/` to `docs/` excluding `manifest.json`.
